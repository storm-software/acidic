# CLI Reference

Acidic is operated via the CLI by running the `acidic run` command. To stop it,
press `ctrl-c`. You may need to wait a bit as acidic completes cleanup tasks.

However, Acidic has more commands:

{{#include ./SUMMARY.md}}
