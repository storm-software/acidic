/**
 * The daemon library used by Storm Software for building TypeScript applications.
 *
 * @remarks
 * A package containing the Acidic Engines daemon process code to allow auto-updated code/artifacts
 *
 * @packageDocumentation
 */

const variable = "daemon";
